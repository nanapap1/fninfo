services:
  rabbitmq:
    image: rabbitmq:4.2-management-alpine
    ports:
      - "5672:5672"
      - "15672:15672"
    env_file:
      - ./.env
    volumes:
      - ./rabbitmq/data:/var/lib/rabbitmq

  postgresql_info:
    image: postgres:18-alpine
    ports:
      - "5434:5432"
    env_file:
      - ./.env
    environment:
      - POSTGRES_DB=${POSTGRES_DB_INFO}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD_INFO}
    volumes:
      - ./postgresql/info:/var/lib/postgresql

  redis_tg:
    image: redis:8.6.2-alpine
    ports:
      - "6379:6379"
    command: ["redis-server","--requirepass","${REDIS_HOST_PASSWORD_TG}"]
    env_file:
      - ./.env
    volumes:
      - ./redis/tg:/data

  epic_api:
    image: ghcr.io/nanapap1/epicapi-service:sha-aa99e409d7025a527f0ee914eb2b9f831c65a57a
    ports:
      - "8086:8086"
    env_file:
      - ./.env

  info:
    image: ghcr.io/nanapap1/info-service:sha-4fd6bd92f79ddcc7fe9a12933549fc815724976d
    ports:
      - "8087:8087"
    env_file:
      - ./.env

  tg:
    image: ghcr.io/nanapap1/tg-service:sha-4fd6bd92f79ddcc7fe9a12933549fc815724976d
    ports:
      - "8089:8089"
    env_file:
      - ./.env

